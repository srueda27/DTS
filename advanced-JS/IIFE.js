var script = (function(param){
  param = 0;
})(parameter)
/* 
  //the second () is the inmediate invocation but you can pass
    down parameters through that 

  And recieve them inside the function with the name we want

  This way we can access other variables from outer scopes,
    without modifying them 
*/