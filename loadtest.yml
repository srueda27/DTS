config:
  target: 'http://localhost:3000'
  phases:
    - duration: 60
      arrivalRate: 10
      name: "Steady state"

scenarios:
  - flow:
    - post:
        url: "/number-generator"
        json: {}